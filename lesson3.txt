When would you want to use a remote repository rather than keeping all your work
local?

    It can be accessed anywhere and is great for group work and sharing code

Why might you want to always pull changes manually rather than having Git
automatically stay up-to-date with your remote repository?

    Code can be pulled when you are at the end of a particular project.

Describe the differences between forks, clones, and branches.  When would you
use one instead of another?

    A branch is a part of a repository that breaks off from the master branch. A clone is when you make a new repository that is the exact same as the existing, you can clone locally or from github to local. A fork is a cloned repository from github to github that you can then clone to your local.

What is the benefit of having a copy of the last known state of the remote
stored locally?

   You can update your copy of work if you aren’t ready to merge the changes into your branch. You can perform the merge offline or if you need the remote you can access it

How would you collaborate without using Git or GitHub?  What would be easier,
and what would be harder?

	We could use a shared folder like drop box or some other file sharing website but it would be very labor intensive to see the changes made if we were all working on the same aspect. 

When would you want to make changes in a separate branch rather than directly in
master?  What benefits does each approach have?

    Fill in your answer here
